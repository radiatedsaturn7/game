{
  "name": "robtergeist",
  "private": true,
  "scripts": {
    "prepare:playwright": "npx playwright install --with-deps",
    "test:dashboard": "npm run prepare:playwright && node tools/test-runner/run.js"
  }
}
